
# File environment.yml
# Run with: conda env create -f environment.yml

name: my_environment
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - tqdm
  - tensorboard
  - pip:
      - matplotlib
      - opencv-python
      - nibabel
      - SimpleITK
      - scipy
      - pandas
      - seaborn
      - monai
